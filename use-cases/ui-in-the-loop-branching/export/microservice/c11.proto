syntax = "proto3";
package ui_in_the_loop_branching;

// Node C1 of the branching human-in-the-loop demo.
service NormalizeService {
  rpc Normalize(NormalizeRequest) returns (NormalizeResponse);
}

message NormalizeRequest { string payload = 1; }
message NormalizeResponse { string result = 1; }
